# This project is created using CRA and use below steps to run locally.
## Available Scripts

In the project directory, you can run:
### `1. Clone the Repository`
### `2. npm install`
### `3. npm start`


Now the app would be running locally on 3000 PORT.


